import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata = {
  title: "Mibp.dev — AI Chat Dashboard",
  description:
    "Mibp.dev AI Chat Dashboard — Bring your ideas to life with Image Generator, Video Generator, and Dev Assistant.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body className={inter.variable}>{children}</body>
    </html>
  );
}
